# dream.md — DDD task note

Outcome: viral-ready local tool `dream-md` — Jev-graded, verbatim-provenance memory for coding agents.
Method: DDD. Plan: `docs/PLAN.md`. Domain: `docs/DOMAIN.md`. Team: `.team/MISSION.md`.
Status: plan v2 in force; M0 done, M1 v2 done, M2 (judgment) done, M3 (memory v2: fact lifecycle, dedupe, FTS retrieve+re-rank, receipts) done, M4 (audit: memory-file parser, Phase C, rules, report, engine) done, M5 (dream engine: phases A/B composed, verdict rules, ask expiry + resolve, dream.md writer) done — 399/399 tests offline; M6 (integration: hooks/CLI) next. Implementation delegated to GLM (M2→M7), review by Kimi.

## Documentation basis

| Question | Source and applicable version/section | Rule and implementation decision | Check/result or open question |
|---|---|---|---|
| Jev API request/response contract | docs.typesafe.ai/api.md (fetched 2026-09-19; distilled at `docs/reference/typesafe-api.md`) | POST /v1/systemone, Bearer auth, model `jev-latest`, questions map with typed answers under same ids | pending live smoke test |
| Question types + answer shapes | docs.typesafe.ai/api.md + /primitives.md | Noul→probability (no confidence); Choice→choice+probabilities+confidence; Score→score+legend+probabilities+confidence | pending |
| Can Jev generate/summarize text? | docs.typesafe.ai/concepts/how-to-build-with-system-one.md — "does not generate code or choose its own next action" | **No generation in dream.md.** Facts = verbatim quotes, selected+graded by judgments. Invariant #1 | shapes design; pending code |
| Batching/cost | docs.typesafe.ai/patterns/fan-out.md + /primitives.md (parallel questions section) | Speculative fan-out: all questions in one request, parallel eval, ~no latency cost; ~32k token budget shared state+questions (~150k chars) | batcher needs budget estimator + tests |
| Confidence semantics + gating | docs.typesafe.ai/confidence.md | Three ranges (act/caution/don't); thresholds scale with risk; destructive ops gated higher | thresholds in `thresholds.py`, Kimi to attack values |
| Retry policy | docs.typesafe.ai/api.md errors section | 401/422 fail fast; 429/529 exponential backoff; hook must never block a session | pending |
| Claude Code transcript format | OBSERVED 2026-09-19 (GLM): 15 real files `~/.claude/projects/**/*.jsonl` (372 user / 526 assistant / 18 system lines) | JSONL; message lines `type`=="user"/"assistant" with `message.{role,content}`; user `content` = str (typed prompt) or block list (tool_result only observed, never a statement); assistant blocks text/thinking/tool_use — only `text` extracted; line fields `sessionId`+`uuid`, `timestamp` (ISO-8601 Z), `cwd`, `isSidechain` (sidechain `agent-*.jsonl` files carry the PARENT sessionId — field authoritative, not filename); user skips: `isMeta`==true, `promptSource`=="system", `origin.kind`=="task-notification"; non-message line types (mode/attachment/last-prompt/permission-mode/atis-latch/file-history-*/ai-title/queue-operation/cost-state/system) ignored | `dream_md/ingestion/claude.py`; verified against all 15 real files + sanitized fixture; 11 parser tests green |
| Codex transcript format | OBSERVED 2026-09-19 (GLM): 1,077 real files `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl` | lines `{timestamp, ordinal, type, payload}`; line 1 always `session_meta` (payload `session_id`, `cwd`, …; filename uuid ≠ session_id in 457/1,077 subagent threads → id always from payload); statements only from `response_item` payload.type=="message" roles user/assistant, blocks `input_text`/`output_text` (`input_image` ignored); role `developer` = machine instructions, skipped; observed machine-injected user wrappers skipped: `<environment_context>` (378), `<subagent_notification>` (312), `# AGENTS.md instructions` (214), `<heartbeat>` (136), `<skill>` (41), `<turn_aborted>` (30) per 200-file sample; `task_complete.last_agent_message` duplicates assistant text → skipped | `dream_md/ingestion/codex.py`; verified against sampled real files + sanitized fixture; 12 parser tests green |
| Claude Code hook surface | Claude Code hooks docs (SessionEnd, stdin JSON with session_id/transcript_path) — verify against local `~/.claude/settings.json` examples | `install` emits hook JSON, never auto-patches without `--yes` | pending — verify real hook payload shape |
| Codex notify surface | Codex config.toml notify (session/turn events) | emit TOML snippet | pending |
| Competitor gap | GitHub/web search 2026-09-17/18 (lead session): Claude Code built-in auto-memory (Feb 2026, no confidence/provenance), agentmemory (5k★, local, no consolidation), eidetic/ai-memory/msync (none use Jev, none calibrated) | differentiation is receipts + calibration, not storage | one more fresh search before launch |
| Redaction at rest (DOMAIN #6, invariant 6) | PLAN v2 decision 5 + DOMAIN v2 invariant 6 + OBSERVED 2026-09-19: 32 secret-shaped occurrences across the 8 pinned real smoke files | `dream_md/ingestion/redact.py`: 9 deterministic patterns applied in order — PEM block, `Bearer …` (runs before JWT so a Bearer-wrapped JWT is consumed once), `sk-…` (≥16 body), AWS `AKIA`/`ASIA` (20-char), `gh[pousr]_` (≥36), JWT (3 dot-segments ≥8), credential assignments (`password/pwd/secret/token/api_key/apikey/access_key` =\|:\|quoted, value ≥8, stops at URL `&`), hex run ≥20 (deliberately over-redacts 40-char commit SHAs — safe side), base64ish ≥20 with digit guard (pure-alpha runs are words). Parsers stay raw; redact happens ONLY in `eventlog.append` (storage boundary); idempotent | 31 tests `tests/test_redact.py`; smoke: 32 replacements in real corpus, 0 raw leaks in store (pinned) |
| SQLite store schema (v2) | PLAN v2 "SQLite schema (v2)" + review R7 (centralize DDL) | ALL DDL in `dream_md/memory/schema.py` (events/facts/fact_links/judgments/runs/facts_fts FTS5 external-content + schema_version); `connect()` = WAL + busy_timeout=5000 on every connection; `migrate()` branches: fresh → create+stamp 2; v1-era (events table, no version row) → keep v1 events as-is (nullable `text` cannot be ALTERed in SQLite; all dream.md writes always supply text — documented tolerance), create v2 tables around it; stamped >2 → `SchemaVersionError` (refuse, never corrupt) | 7 tests `tests/test_schema.py`; DDL-centralization asserted by test (eventlog owns no CREATE); FTS5 wiring verified |
| Event id scheme (v2) | PLAN v2 + review R2 (MAJOR) | `event_id = sha256(session_id + "\x1f" + normalize_content(redacted text))` — exact scheme incl. separator pinned by test. Idempotent under in-session transcript rewrites (compaction/reflow); cross-session verbatim repeats SURVIVE as distinct events (M3 bumps support_count); same-session repeats collapse via INSERT OR IGNORE (one support source per session; sidechain/main dupes collapse since sidechains carry the parent session id) | `tests/test_eventlog.py`: sha256 equality pinned, 2-thread concurrent ingest green under WAL |
| Candidate extraction (v2) | PLAN v2 ingestion spec + reviews R2 (no dedupe) | sentence-boundary greedy packing 280–600 (cap hard: oversize sentences whitespace-split, hard cut if no space); anaphoric-start drop (that/this/it/they/"the same"/those, case-insensitive) on ALL chunks including a statement's first — pinned stance; verbatim context = 1–2 preceding user/assistant turns of the SAME session, `[role] text`, redacted, ≤800 chars cut at word boundary; NO content dedupe — every occurrence is a candidate with its own event id (dedupe is storage-level for same-session, memory-level for cross-session) | `tests/test_extract.py`; smoke: 357 candidates / 147 statements, max len exactly 600, contexts ≤800, 0 violations |
| Reproducible smoke | review R6 | `scripts/corpus_smoke.py`: pinned explicit 8-file list (4 Claude incl. one sidechain + 4 Codex incl. both fixture-origin sessions), no globbing; reads only, no network/API key; exit 1 if no pinned file parses | 2026-09-19 run: 147 statements, 357 candidates, 143 unique event ids, roles assistant=257/user=100, 32 secret replacements, 0 cap/floor violations |
| Judgment question set (Phase A/B) | PLAN v2 M2 section | Phase A speculative triple per candidate: durable (noul), category (choice, 6 options), significance (score, 4 levels with rubric criteria); Phase B pair triple vs each retrieved fact: same_claim (noul), contradicts (noul), verdict (choice: new_overrides/old_stands/unclear). Instructions are complete standalone questions pointing at backticked state paths; question ids are code-only keys, never shown to the model | `dream_md/judgment/questions.py`; vocabularies pinned exactly by 18 tests `tests/test_questions.py` (option sets, rubric-led level names, ids never in instructions, per-batch re-indexing) |
| Typed answer parsing | docs/reference/typesafe-api.md primitives section (examples used verbatim in tests) | `parse_answer()` strict: noul ∈ [0,1] (no confidence by spec, extra fields ignored); choice ∈ options + probabilities sum 1 ±0.01 + confidence ∈ [0,1]; score ∈ legend level range + probabilities + confidence; type mismatch / non-object / off-spec → `JevProtocolError` (surfaced, never guessed); unknown expected type → ValueError (programming error) | 22 tests `tests/test_answers.py` built on the reference's own example payloads |
| Jev client + retry | docs/reference/typesafe-api.md errors section | `JevClient` with injectable transport/sleep/jitter (stdlib default): 401 → `JevAuthError` fail fast; 422 → `JevValidationError` fail fast with body detail; 429/529 → retry ≤3 attempts on 1s/2s/4s backoff + ≤0.25s jitter then `JevRetryExhausted` (a memory hook never crashes a session); other HTTP / URLError / OSError → `JevTransportError` fail fast; strict envelope validation (answer per asked id, integer usage, non-empty model echo) | 17 tests `tests/test_client.py` — offline scripted transport; backoff schedule, jitter, exhaustion, fail-fast codes, envelope edges all pinned |
| Batching (fan-out budget) | docs.typesafe.ai/patterns/fan-out.md + PLAN M2 | pessimistic chars/4 estimator over state + ALL question objects (ids count — they are request keys); greedy packing in original order under `BATCH_TOKEN_BUDGET` 28k; floor = one candidate per request (per-candidate fallback); single candidate + context over budget → `BatchError` (never send an over-budget request); compositional char math re-verified with the real estimator on every emitted batch (drift = internal error) | 10 tests `tests/test_batch.py` incl. synthetic 300/500-candidate loads: coverage exact, order preserved, every batch ≤ budget |
| Offline judgment (FakeJev) | PLAN M2 offline-test requirement | duck-typed `ask()`; normal mode decisive (0.9 nouls, peaked distributions, conf 0.8) feeds happy-path pipeline; adversarial mode exact-0.5 nouls + flat distributions + 0.0 confidence lands in every gate's uncertain band (queue/ask-later logic); scripted mode raw answers through the SAME strict parser (fixtures cannot drift off-spec); error injection by 1-based call number or `"always"`; requests recorded for assertions | 16 tests `tests/test_fake.py`; whole suite runs with no network, no API key |
| Fact lifecycle (M3) | PLAN v2 M3 verdict section (keep/supersede/retire/ask) + F7.2 ask-expiry stance | `add_fact` inserts ACTIVE (confidence = the one formula) + FTS index; duplicate → `bump_support` (appends source event id, count +1, idempotent per event id — one support source per session); destructive → `supersede` (status + FTS delete + `supersedes` link) / `retire` (status + FTS delete, row kept for provenance); low-confidence conflict → `mark_ask` (resets ask_seen_count) + `bump_ask` (counts only ask-status facts; other statuses no-op; expiry disposition is M5's); missing ids raise ValueError | `tests/test_facts.py` lifecycle class + add/dedupe/retrieve/receipts classes; 29 tests |
| Code-level dedupe (zero Jev spend) | PLAN v2 M3 dedupe + `JACCARD_GATE` 0.6 (thresholds.py) | normalized-claim sha256 equality → 1.0; else best token-set Jaccard ≥ gate among ACTIVE facts; tokens = lowercase alphanumeric ≥2 chars; empty token sets never match. Python scan over all active rows is DELIBERATE (store is per-project so the scan IS the project scope; exact hash must hold even for claims with no FTS-indexable tokens; hundreds of facts make it trivial) — flagged for revisit only if real corpora prove slow | hash-equality (case/whitespace) + Jaccard-above-gate + below-gate-None + best-match-wins pinned; retired facts never match (would-match sanity asserted) |
| FTS retrieve + re-rank (Phase B partners) | PLAN v2 M3 + `FTS_RETRIEVE_K`=10, `FTS_KEEP_TOP`=5 (thresholds.py) | FTS5 OR-match over claim tokens fetches top k by bm25 rank, re-rank by token-set Jaccard, keep top 5; ties break to the OLDEST fact (stable partner choice); empty-token queries return []; `facts_fts` is external-content FTS5 (`content='facts', content_rowid='id'`) so MATCH consults the index only — deleted rowids stop matching while the facts row survives; retire/supersede issue the external-content 'delete' command with the EXACT indexed claim | 6-fact seeded corpus pins rank order + tie-break + only-active; k/keep bounds test; FTS healthy after deletes (no ghosts, remaining rows still served) |
| Store receipts (judgments + runs) | PLAN v2 receipts + review F7.1 (every number reproducible from the store) | `record_judgment` stores the verbatim answer JSON keyed by run + subject (kind/id) + question id, indexed on (subject_kind, subject_id); `start_run`/`finish_run` bracket every dream/audit run with stats JSON (api_calls, usage, dropped_low_durable…) or an error string — a run row exists even when a run fails | roundtrip tests assert verbatim answer_json equality + stats/error branches |
| Memory-file line parser (M4) | PLAN v2 M4 audit spec + OBSERVED dream.md/CLAUDE.md conventions | `dream_md/audit/memfile.py`: one FILE line = one auditable line; auditable = list items (any marker, any indent) + plain prose; skipped = blank, ATX headings (tracked as section context, never claims), HRs, fenced code (content + markers; closer = marker char-set match ONLY, so ```python inside a fence stays content), tables, empty list markers, HTML comments (removed inline anywhere in a line — the dream.md sentinel never gets audited; comment-only lines vanish via the empty-claim check); inline markup stripped in order: comments → links (keep text) → bold (`\*{2,}` so `***x***` resolves in ONE match, no leftover star) → italic → code → space-run collapse; line identity = `line:<file line number>` — stable receipt id that survives text edits on other lines | 19 tests `tests/test_memfile.py` (every stance incl. tilde/backtick fence cross-immunity, triple-star, comment+claim same line) |
| Phase C questions + audit batching (M4) | PLAN v2 M4 + docs.typesafe.ai/patterns/fan-out.md (shared state = per-request overhead) | disposition vocabulary pinned `keep/stale/wrong/unsupported`; per-line triple: supported (noul), contradicted (noul), disposition (choice over the 4 options); evidence pool = most recent `AUDIT_EVIDENCE_FACTS`=30 active facts + `AUDIT_EVIDENCE_STATEMENTS`=40 most recent events (thresholds.py), redacted at rest already, presented oldest-first, and unlike Phase A it is SHARED OVERHEAD in EVERY request (the audit's whole point is grading lines against evidence); two-layer batching identical to Phase A discipline: greedy compositional chars/4 fill in original order under 28k, per-batch re-verification with the real estimator, floor = one line per request, line+evidence overflow → `BatchError`; `l{i}_` position ids reset per batch and map back via `line.id` (candidate_ids carries them) | 21 tests `tests/test_audit_questions.py` + `tests/test_audit_batch.py` (vocab exact, ids never in instructions, state shape exact, evidence shared, synthetic 300-line load, tight-budget one-line-per-request, overflow) |
| Disposition rules + report renderers (M4) | PLAN v2 "Decision rules" + screenshot-shaped terminal report spec | rules are CODE not Jev: gate `AUDIT_DISPOSITION_GATE`=0.6 on the choice confidence — decisive → keyword KEEP/STALE/WRONG/UNSUPPORTED, below → REVIEW band (surfaced and counted, never silent — same stance as Phase A near-misses); the two nouls are RECEIPTS reported verbatim beside the keyword, never gates; `count_dispositions` by keyword; renderers all deterministic fixed-width: terminal = demo-voice summary one-liner + aligned table of PROBLEM lines only in file order + receipts footer (all-keep files get no table), md = full table of every line with escaped pipes, json = every number traceable to judgments receipts; `build_report` derives counts, never hand-passed | 14 tests `tests/test_audit_rules.py` (gate bands, boundary-at-gate, custom gate, verbatim nouls, bad-answer errors) + 15 tests `tests/test_audit_report.py` (pinned column layout 0:4/6:17/19:23/25:29/31:37/39:, clip, md/json shapes, counts-derived) |
| Audit engine (M4) | PLAN v2 M4 engine spec | `run_audit` composes parse → evidence pool → plan → ask → verdicts → receipts → run row; asker contract = any object with `ask(state, questions)` (JevClient live, FakeJev offline); zero auditable lines → ZERO SPEND (no run row, no api calls, empty report); evidence pool scoped per project, ACTIVE facts only, capped + recency (highest fact id / highest events rowid), oldest-first; receipts = 3 per line with STABLE question keys `supported`/`contradicted`/`disposition` (position `l{i}_` ids would collide across batches; subject_id line:<n> already disambiguates) and answer_json in the WIRE shape (type + parsed fields reconstructed); run row kind 'audit' with stats api_calls/usage/lines_audited/lines_<keyword>; on `JevError` → finish_run(error) + re-raise — the CLI (M6) owns exit codes, nothing swallowed | 9 end-to-end tests `tests/test_audit_engine.py` (normal all-keep, adversarial all-REVIEW, scripted mixed dispositions + summary, zero-spend, evidence caps/recency/project-scope/retired-excluded, multi-batch position-reset mapping, error path); suite 319/319 offline |
| Dream engine (M5) | PLAN v2 M5 spec + reviews F7.1/F7.2 + thresholds.py | `run_dream` composes gate → drain → cap → Phase A → code-dedupe → Phase B → ask bump/expiry → writer. Privacy gate: missing opt-in marker → `GradingNotEnabledError`, no run row, no store trace. Queue drain: pending events re-derived into candidates, grouped by normalized text + role (cross-session repeats grade ONCE, support bumps per occurrence); events yielding no candidates are graded too (never re-queued). Cap `MAX_CANDIDATES_PER_DREAM` (default None): user-role-first, events past it stay pending (`events_deferred`). Phase A under the shared 28k batch budget (multi-batch position mapping pinned at 120 fat candidates); verdicts apply durable/significance gates; code-dedupe (hash or Jaccard ≥0.6) consumes BEFORE any pairing (zero Jev spend, bump support); pairing only at significance ≥ `PAIR_SIGNIFICANCE_GATE`=2 (v1 cost cap — below the bar adds directly). Run row kind 'dream' with stats (api_calls, usage, drops, near-misses, adds, pairs, asks); `JevError` → finish_run(error) + re-raise, events stay pending, next dream recovers — nothing swallowed, CLI (M6) owns exit codes | 23 end-to-end tests `tests/test_dream_engine.py` (gate, drain + dedupe-across-sessions, cap ordering, verdict bands, code-dedupe, every Phase B action, duplicate-beats-supersede across partners, ask lifecycle, expiry, multi-batch 120-candidate coverage, error path + recovery) |
| Phase A/B verdict rules (M5) | PLAN v2 "Decision rules" + thresholds.py | Rules are CODE not Jev (same stance as M4). Phase A: durable gate by role — user ≥0.7 (decisions), assistant ≥0.8 (narration); significance ≥1.0; near-miss band [0.5, gate) surfaced + counted, never silent; answers kept VERBATIM on the verdict object (receipts + status). Phase B `pair_action` priority: same_claim ≥0.8 → duplicate (bump support, no new fact); else contradicts ≥0.6 + verdict `new_overrides` + confidence ≥0.8 → supersede; else conflict not decisively overridden → ask; else none (unrelated, plain add). Duplicate beats supersede when different partners fire different actions — support-bumping is always safe, destructive verdicts are not | 17 tests `tests/test_dream_rules.py` (every gate boundary, priority order, durable-drop beats significance, verbatim answers, stable receipt keys) |
| Phase B pair batching (M5) | PLAN M2 fan-out discipline + PLAN v2 Phase B | `plan_phase_b` reuses the audit-layer two-layer discipline: pair triples (`p{i}_{j}_same_claim/contradicts/verdict`) packed greedily under 28k with the real chars/4 estimator + per-batch re-verification; the candidate + ALL its pair facts are one group so a candidate's pairs never straddle requests; retrieved facts are a SHARED per-batch facts array (deduped, id-keyed) — the fan-out "shared state" pattern applied to partner facts; duplicate-pair guard: one candidate × one fact appears once per batch; `p{i}_{j}` position ids map back via (candidate_ids, fact_ids) | 13 tests `tests/test_dream_batch.py` (order, coverage, shared-facts dedupe incl. multi-plan spans, pair positions, budget re-verification, 300-pair synthetic load, tight budget, overflow, non-positive budget) |
| Ask lifecycle + resolve (M5) | PLAN v2 + review F7.2 (ask expiry stance) | Asks are held rows, never dropped silently: `mark_ask` on low-confidence conflict; EVERY dream bumps open asks (`bump_and_expire_asks`); at `ASK_EXPIRY_DREAMS`=3 consecutive unresolved dreams the challenger RETIRES keep-old (incumbent stays active) with an `expiry` receipt under the dream's own run_id (provenance: the expiry is reproducible from the store). `resolve(conn, fact_id, disposition, …)` is the human path: `keep_new` → challenger active + incumbent superseded + `supersedes` link + resolve run row + receipt; `keep_old` → challenger retired, incumbent untouched; receipt confidence 1.0 BY FIAT (the human IS the certainty source); errors: unknown fact, bad vocabulary, non-ask fact, challenger without incumbent | 10 tests `tests/test_dream_resolve.py` (keep_new/keep_old end-to-end, receipt-by-fiat, bump counts, expiry-at-limit with incumbent intact, queue empties, error cases) |
| dream.md writer + sentinel (M5) | PLAN v2 dream.md spec + DOMAIN #2 (agent-honored file) | `render_dream_md` deterministic pure function: sentinel comment first line (versioned, `-- do not edit`, regenerate instruction); pinned category sections in CATEGORY_ORDER (the 6-option vocabulary minus `none` — none-categorized facts intentionally stay OUT); within a section sort significance desc → confidence desc → id; one-line entries `- **"claim"** (source/…)· support · significance x/3 · confidence y · stale badge`; claim newlines flattened for display (store stays raw); staleness badge at `STALENESS_BADGE_DAYS`=30 is VISUAL-ONLY (no decay, DOMAIN #8); UNKNOWN categories get sections too — data drift is rendered, never hidden; open asks render in their own section with both claims + disposition options. `write_dream_md` sentinel guard: fresh write ok, regenerate byte-identical, foreign file or unknown/older sentinel → `SentinelError` refuse (never clobber a file we didn't write); `--force` override is M6 CLI's call | 17 tests `tests/test_dream_writer.py` (layout, pinned order, sort, receipt-line formula, clamping, newline flattening, unknown category, asks section, byte-identical purity; sentinel fresh/regenerate/foreign/older-version/force) |

## Progress log

- 2026-09-19 00:56 — repo created, plan + domain + API reference written, team dispatched (GLM: M0+M1 first; Kimi: plan attack). Implementation/checks pending.
- 2026-09-19 01:04 — M0 done (GLM): `dream_md/` package, `pyproject.toml` (name dream-md, console script `dream-md = "dream_md.cli:main"`, zero runtime deps), stdlib-only AST gate + packaging/CLI tests, unittest harness; `.gitignore` pre-existed from lead. 4/4 tests green offline. Commit `4e82091` — includes lead-authored docs/ + .team/ + .ddd/ baseline so history starts complete (flag to lead: OK to fold into initial commit?).
- 2026-09-19 01:08 — M1 done (GLM): parsers built from OBSERVED schemas (rows above; 15 Claude + 1,077 Codex files inspected on this machine before coding); event log writer (events table per PLAN v1, per-project store `~/.dream-md/projects/<sha1-12>.db`, re-ingest idempotent; **event id = sha256("<abs transcript path>:<line_no>:<statement index>")** — plan said path+line only, index added because one line can yield several statements and ids must stay unique); deterministic candidate extraction (verbatim chunks ≤600 chars, whitespace-boundary breaks, sha256 content-hash dedupe first-occurrence-wins, MIN 12-char floor vs observed filler like "keep going" — tunable constants). Fixtures sanitized from observed shapes (no secrets). 52/52 tests green offline (`python3 -m unittest discover`, no network, no API key). Real-corpus smoke: 15 Claude + 11 sampled Codex files → 335 statements (43 user / 292 assistant), 769 candidates, all ≤600 chars, unique hashes. Open: (1) Claude sidechain statements attributed to parent session id (observed behavior, kept — agent dispatch prompts are still agent statements); (2) Codex user wrapper list covers the six OBSERVED tags only (named constant, extensible); (3) real-corpus candidates skew assistant-heavy (292/335) — Phase A grading is the quality filter, per plan.
- 2026-09-19 ~01:25 — LEAD adjudication of Kimi's plan attack (`.team/kimi/findings-plan.md`): ALL findings accepted (F1.1 implemented as context-field rather than assistant-exclusion; F7.2 as ask-expiry + resolve). PLAN.md rewritten as **v2** — see its "v2 changes" list. DOMAIN.md invariants 6–8 added (redaction at rest, grading opt-in, no decay). Milestones reordered: audit (M4) before dream engine (M5). Answer to GLM's flag: yes, folding lead docs into commit 4e82091 is fine — keep history as-is. M1-affecting v2 deltas for GLM: redaction pass before storage, sentence-boundary chunking + anaphoric-start drop, verbatim context capture (1–2 preceding exchanges, ≤800 chars), event-id base switched to normalized content (constraint: ids must survive transcript file rewrites without duplicating events; identical content collapsing across sessions is acceptable — GLM may combine content-hash with statement index to preserve uniqueness), WAL + busy_timeout on every connection, partial-tail line tolerance. Codex notify surface verified by lead locally: `notify = ["…", "turn-ended"]` argv invocation, codex-cli 0.154.0, `[features] hooks=true` — exact payload JSON still open; mitigated by `--scan` discovery (PLAN v2).
- 2026-09-19 ~15:40 — M1 v2 deltas DONE (GLM, PART 1 of lead's 3-part slice; Kimi's M0/M1 review findings folded in, `.team/kimi/findings-m01.md`): **R2** event_id = sha256(session_id + "\x1f" + normalized redacted content) — rewrites/compaction idempotent, cross-session repeats survive, same-session repeats + sidechain/main dupes collapse; **R5** `realpath` for project slug + store paths (symlink spellings collapse); **R3** both fixtures re-sanitized (/Users/dev/example paths, synthetic 0000…/1111… session UUIDs, redaction-corpus sample line added); **R6** `scripts/corpus_smoke.py` pinned 8-file list; **R7** all DDL centralized in `dream_md/memory/schema.py` with schema_version + migrate branches (fresh / v1-era keep-as-is / future refuse); **R8** unknown-injection passthrough test (denylist is observed-only — pinned stance, allowlist would drop sidechain dispatch prompts); **R10** `MAX_CANDIDATES_PER_DREAM` TODO in thresholds.py for M2 to wire. New modules: `thresholds.py` (all named tunables), `memory/schema.py`, `ingestion/redact.py` (9 patterns, idempotent, sequential application). Rewritten: `eventlog.py` (redact-at-rest, schema delegation, WAL), `extract.py` (sentence chunking 280–600, anaphoric drop ALL chunks, per-session context ≤800 redacted, NO content dedupe), both fixtures, `models.py` docstrings. 119/119 tests green offline (`python3 -m unittest discover`; no network, no API key). Real-corpus smoke (pinned): 147 statements, 357 candidates, 143 unique event ids, max candidate len 600, 32 secret replacements, 0 violations. Flags for lead: (1) anaphoric drop applies to a statement's FIRST chunk too — pinned in test, one-line change if you want exemption; (2) redaction deliberately over-redacts 40-char hex (commit SHAs) — deterministic safe side; (3) v1-era stores keep nullable events.text (SQLite cannot ALTER) — documented tolerance, all writes supply text; (4) same-session verbatim repeats collapse to one event by design (one support source per session) — if you want per-occurrence receipts, it's an id-scheme change; (5) commit-time secret scanner blocked literal rule-shape samples — test vectors are now assembled at runtime from parts (no complete secret-shaped literal anywhere in the tree; rg-verified) and the fixture's sample line is a placeholder credential assignment only — rule coverage unchanged, 31 redaction tests still pin every pattern.
- 2026-09-19 02:00 — M2 judgment DONE (GLM, PART 2 of 3): package `dream_md/judgment/` — `errors.py` (JevError hierarchy: Auth 401 / Validation 422 / RetryExhausted 429+529 with last_error / Transport / Protocol), `answers.py` (Noul/Choice/Score frozen dataclasses + strict `parse_answer`, probability-sum tolerance ±0.01, legend-range checks), `questions.py` (Phase A speculative triple + Phase B pair triple + state builders + pinned vocabularies), `tokens.py` (compact sort_keys char counting, chars/4 estimator), `client.py` (injectable-transport JevClient, fail-fast 401/422/other, 429/529 retry 1s/2s/4s + ≤0.25s jitter then graceful exhaustion), `batch.py` (greedy 28k-budget planner with compositional estimation + per-batch re-verification + per-candidate fallback floor), `fake.py` (FakeJev normal/adversarial/scripted + error injection + request recording). `thresholds.py` extended: CHARS_PER_TOKEN=4, BATCH_TOKEN_BUDGET=28_000, RETRY_MAX_ATTEMPTS=3, RETRY_BACKOFF_SECONDS=(1,2,4), RETRY_JITTER_MAX=0.25, REQUEST_TIMEOUT_SECONDS=30. **212/212 tests green offline** (93 new: answers 22, questions 18, client 17, tokens 10, batch 10 incl. synthetic 300/500-candidate loads, fake 16). Zero network, zero API key in the whole suite — every client behavior driven by a scripted injected transport. Flags for lead: (1) FakeJev normal-mode significance lands at 1.2 (clears the ≥1 gate, under 2) and adversarial at exact midpoint 1.5 — deterministic by design; the adversarial mode exists to sit in every gate's uncertain band and it does (0.5 < all of 0.6/0.7/0.8); (2) estimator is deliberately pessimistic chars/4, no tokenizer dependency — overshoot is safe, undershoot would waste a 422; (3) batch planner re-verifies each emitted batch with the real estimator and raises BatchError on drift — internal-consistency tripwire, not expected to fire; (4) scripted FakeJev answers go through the same strict parser as real envelopes, so off-spec fixtures fail loudly (pinned by test).
- 2026-09-19 ~02:30 — M3 memory DONE (GLM, PART 3 of 3): `dream_md/memory/facts.py` — frozen `Fact` dataclass + full lifecycle (add_fact ACTIVE+FTS index / bump_support idempotent per event id / supersede with link / retire / mark_ask resets count / bump_ask ask-only counting), code-level dedupe (`find_code_duplicate`: normalized sha256 equality → 1.0, else best token-set Jaccard ≥ 0.6 among ACTIVE facts), FTS retrieve + re-rank (`retrieve_similar`: FTS5 top-10 bm25 → Jaccard re-rank → keep 5, ties to oldest), receipts (`record_judgment` verbatim answer JSON; `start_run`/`finish_run` stats-or-error), `add_link` INSERT OR IGNORE. `memory/__init__.py` exports the full M3 API. **241/241 tests green offline** (29 new in `tests/test_facts.py`, temp-db-per-test, all timestamps pinned). Flags for lead: (1) dedupe gate is LEXICAL — "use npm not bun" vs "use bun not npm" is 0.857 similar at word-set level and consolidates; catching semantic contradiction is Phase B's job (by design, pinned by test); (2) `facts_fts` is external-content FTS5 — a plain `SELECT … WHERE rowid=?` reads through to the facts table, so index state must be asserted via MATCH (tests do); (3) only ACTIVE facts participate in dedupe AND retrieval — ask-status facts are invisible to both while awaiting human resolve; (4) `find_code_duplicate` scans all active rows in Python, O(facts) per candidate — deliberate (exact-hash must hold even for claims with no FTS-indexable tokens; corpora are hundreds), revisit only if slow on real corpora; (5) `bump_support` is idempotent per event id — same-session re-grading never double-counts (mirrors the event-level one-support-source-per-session stance).
- 2026-09-19 ~03:10 — M4 audit DONE (GLM, PART 4): package `dream_md/audit/` — `memfile.py` (parser: list items + prose auditable; headings/HRs/fences/tables/comments skipped; inline markup stripped; `line:<n>` receipt ids), `questions.py` (Phase C triple + pinned disposition vocab + audit_state with evidence pool), `batch.py` (greedy planner, evidence as shared overhead in EVERY request, same two-layer estimation discipline as Phase A), `rules.py` (0.6 confidence gate → keyword or REVIEW band; nouls verbatim receipts; stable question keys), `report.py` (terminal problem-lines-only table / full md table / json, all deterministic; counts derived), `engine.py` (`run_audit`: zero-spend on empty files, per-project capped evidence pool, client.ask contract, run row + 3 receipts per line, JevError → finish_run(error) + re-raise). `thresholds.py` extended: `AUDIT_EVIDENCE_FACTS`=30, `AUDIT_EVIDENCE_STATEMENTS`=40, `AUDIT_DISPOSITION_GATE`=0.6. **319/319 tests green offline** (78 new: memfile 19, questions 11, batch 10, rules 14, report 15, engine 9 — engine tests run end-to-end against a real temp store with FakeJev normal/adversarial/scripted). Flags for lead: (1) evidence pool is capped at 30 facts + 40 statements by RECENCY — an audit never reads the whole store; if a memory file needs deeper history the cap is one threshold; (2) REVIEW band is never silent — sub-gate confidence lines are surfaced and counted, mirroring the Phase A near-miss stance; (3) receipts use STABLE question ids (`supported`/`contradicted`/`disposition`) + `line:<n>` subject ids — the `l{i}_` position ids would collide across batches and the stable pair already disambiguates; (4) the terminal table shows PROBLEM lines only (screenshot shape, matches the demo); `--md` renders every line, `--json` every number; (5) HTML comments are stripped inline so the dream.md sentinel line itself can never be audited as a claim; (6) adversarial FakeJev (0.0 confidence) lands every line in REVIEW — the audit equivalent of the M2 uncertain band, exercised by test.
- 2026-09-19 ~04:00 — M5 dream engine DONE (GLM, PART 5): package `dream_md/dream/` — `batch.py` (Phase B pair planner: candidate+facts grouped, shared deduped facts array per batch, pair triples under the 28k budget with re-verification), `rules.py` (Phase A durable/significance gates + near-miss band; Phase B `pair_action` priority duplicate→supersede→ask→none; answers verbatim), `writer.py` (deterministic `render_dream_md` + sentinel-guarded `write_dream_md`), `resolve.py` (`resolve` keep_new/keep_old with by-fiat receipt; `bump_and_expire_asks` keep-old expiry at 3 dreams), `engine.py` (`run_dream` full composition + `DreamReport` + `GradingNotEnabledError`), `__init__.py` (25-symbol API). `thresholds.py` extended: `PAIR_SIGNIFICANCE_GATE`=2 (v1 cost cap per PLAN Phase B #3 — below the bar adds directly). `eventlog.py` +`optin_path`; `facts.py` +`ask_facts`/`contradicts_partner`/`reactivate` (docstring-only updates elsewhere). **399/399 tests green offline** (80 new: batch 13, rules 17, writer 17, resolve 10, engine 23 — engine tests end-to-end against a real temp store with FakeJev normal/adversarial/scripted, incl. a 120-fat-candidate multi-batch run with exact receipt-coverage assertions). Two writer bugs caught by the new tests and fixed: unknown-category facts were collected but never rendered (docstring promised "never hidden" — now they get sections), and a `none`-categorized fact fell into the unknown branch and rendered a `## None` section (none now intentionally stays out per PLAN). Flags for lead: (1) `resolve` receipt confidence is 1.0 BY FIAT — the human is the certainty source; if you want honest receipt semantics for resolves, it's a one-line change; (2) ask expiry is keep-old (incumbent stays active) per F7.2 — the challenger row survives retired for provenance; (3) Phase B pairing bar sits at significance 2 to cap v1 spend — candidates between 1 and 2 add directly with NO pair questions, so early low-signal corpora stay cheap; (4) `_display` flattens `\r\n`→one space (was double) — cosmetic, store untouched; (5) the multi-batch engine test needed 120 mostly-unique-token claims because Jaccard dedupe correctly eats digit-only variants (Jaccard ~0.97) — that's the dedupe working as designed, not a wart.
