"""jev-md: coding-agent memory where every fact is a verbatim quote
graded by calibrated confidence.

Python 3 stdlib only — no third-party runtime dependencies (mission
invariant, enforced by tests/test_packaging.py).
"""

__version__ = "0.1.0"
